# Локальный бэкенд: запросы идут на /api, Vite проксирует на http://127.0.0.1:8000 (vite.config.ts).
# Раскомментируйте строку ниже, если API только на VPS:
# VITE_API_BASE_URL=http://87.242.87.53:8000
